<Window x:Class="DelsysSigNIalGen.View.MultipleNICardSelectView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:viewmodel="clr-namespace:DelsysSigNIalGen.View"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes" 
        d:DataContext="{d:DesignInstance Type={x:Type viewmodel:MultipleNICardSelectViewModel}}"
        mc:Ignorable="d"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        TextElement.FontWeight="Regular"
        TextElement.FontSize="13"
        TextOptions.TextFormattingMode="Ideal"
        TextOptions.TextRenderingMode="Auto"
        Background="{DynamicResource MaterialDesignPaper}"
        FontFamily="{DynamicResource MaterialDesignFont}" Title="MultipleNICardSelectView" Height="250" Width="600">
    <Grid>
        <materialDesign:Card Margin="10">
            <StackPanel Margin="10">
                <TextBox IsReadOnly="True" HorizontalAlignment="Center" Text="Multiple NI cards Detected, Please Select one:" Margin="10" FontSize="25" Foreground="White"/>
                <ComboBox ItemsSource="{Binding NICardsConnected}" SelectedItem="{Binding SelectedNICard}" Margin="10"/>
                <Button Margin="10" Content="Select" Click="Button_Click"/>
            </StackPanel>
        </materialDesign:Card>
    </Grid>
</Window>
